---
name: Functional Tests

on: # yamllint disable-line rule:truthy
  push:
    branches:
      - main
  pull_request:
    paths:
      - .github/workflows/functional-tests.yml
      - src/**
      - action.yml
  schedule:
    # Every Friday at 09:00 JST
    - cron: "0 0 * * 5"
  workflow_dispatch: {}

defaults:
  run:
    shell: sh

jobs:
  setup-whitespace:
    name: Setup whitespace
    runs-on: ${{ matrix.os }}-latest
    timeout-minutes: 5
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu", "macos", "windows"]
    steps:
      - name: Checkout ${{ github.repository }}
        uses: actions/checkout@v7
        with:
          persist-credentials: "false"
      - name: Setup Whitespace
        id: setup-whitespace
        uses: ./
      - name: Test action completion
        env:
          STEP_SETUP_WHITESPACE_OUTPUT_INSTALLED: "${{ steps.setup-whitespace.outputs.installed }}"
        run: |
          test_equal() {
            if [ "${2}" != "${3}" ]; then
              echo "::error title=${1}::Expected: ${3}. Actual: ${2}."
              exit 1
            fi
          }
          test_equal "wspace should be installed" \
            "${STEP_SETUP_WHITESPACE_OUTPUT_INSTALLED}" \
            "true"
      - name: Validate script
        run: |
          [ "$(wspace helloworld.ws)" = "Hello, World!" ] || exit 1;
  setup-whitespace-in-container:
    name: Setup whitespace in container (${{ matrix.image }}, latest)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    container:
      image: ${{ matrix.image }}:latest
    strategy:
      fail-fast: false
      matrix:
        image: ["alpine", "ubuntu"]
    steps:
      - name: Checkout ${{ github.repository }}
        uses: actions/checkout@v7
        with:
          persist-credentials: "false"
      - name: Setup Whitespace
        id: setup-whitespace
        uses: ./
      - name: Test action completion
        env:
          STEP_SETUP_WHITESPACE_OUTPUT_INSTALLED: "${{ steps.setup-whitespace.outputs.installed }}"
        run: |
          test_equal() {
            if [ "${2}" != "${3}" ]; then
              echo "::error title=${1}::Expected: ${3}. Actual: ${2}."
              exit 1
            fi
          }
          test_equal "wspace should be installed" \
            "${STEP_SETUP_WHITESPACE_OUTPUT_INSTALLED}" \
            "true"
      - name: Validate script
        run: |
          [ "$(wspace helloworld.ws)" = "Hello, World!" ] || exit 1;
  test-force:
    name: Test force
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        force: ["true", "false"]
    steps:
      - name: Checkout ${{ github.repository }}
        uses: actions/checkout@v7
        with:
          persist-credentials: "false"
      - name: Setup Whitespace 1
        id: setup-whitespace-1
        uses: ./
      - name: Setup Whitespace 2
        id: setup-whitespace-2
        uses: ./
        with:
          force: ${{ matrix.force }}
      - name: Test action completion
        env:
          STEP_SETUP_WHITESPACE_1_OUTPUT_INSTALLED: "${{ steps.setup-whitespace-1.outputs.installed }}"
          STEP_SETUP_WHITESPACE_2_OUTPUT_INSTALLED: "${{ steps.setup-whitespace-2.outputs.installed }}"
        run: |
          test_equal() {
            if [ "${2}" != "${3}" ]; then
              echo "::error title=${1}::Expected: ${3}. Actual: ${2}."
              exit 1
            fi
          }
          test_equal "Wrong \"installed\" output from setup-whitespace-1" \
            "${STEP_SETUP_WHITESPACE_1_OUTPUT_INSTALLED}" \
            "true"
          test_equal "Wrong \"installed\" output from setup-whitespace-2" \
            "${STEP_SETUP_WHITESPACE_2_OUTPUT_INSTALLED}" \
            "${{ matrix.force }}"
